<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://weilixu.com/feed.xml" rel="self" type="application/atom+xml"/><link href="https://weilixu.com/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-29T05:55:01+00:00</updated><id>https://weilixu.com/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Understanding Activation Memory Dynamics in Pipeline Parallelism Variants</title><link href="https://weilixu.com/blog/2026/pipedream-vs-gpipe/" rel="alternate" type="text/html" title="Understanding Activation Memory Dynamics in Pipeline Parallelism Variants"/><published>2026-02-14T00:00:00+00:00</published><updated>2026-02-14T00:00:00+00:00</updated><id>https://weilixu.com/blog/2026/pipedream-vs-gpipe</id><content type="html" xml:base="https://weilixu.com/blog/2026/pipedream-vs-gpipe/"><![CDATA[<p>This is a pointer blog post to an interactive simulation that visualizes the memory dynamics of <strong>GPipe</strong> (naive pipeline parallelism schedule) vs <strong>PipeDream</strong> (1F1B schedule).</p> <p><a href="https://pipedream-vs-gpipe-visualizer-952909165134.us-west1.run.app/">Launch Interactive Simulation</a></p> <h3 id="conceptual-analysis">Conceptual Analysis</h3> <p>We compare two major pipeline parallelism strategies:</p> <ol> <li><strong>GPipe (Standard):</strong> This approach uses a “flush-based” schedule where all forward passes for a microbatch must complete before any backward passes begin. As the simulation demonstrates, this causes activation memory to accumulate linearly with the number of microbatches, creating high peak memory pressure. <ul> <li><em>Reference:</em> <a href="https://research.google/blog/introducing-gpipe-an-open-source-library-for-efficiently-training-large-scale-neural-network-models/">Introducing GPipe, an Open Source Library for Efficiently Training Large-scale Neural Network Models</a></li> </ul> </li> <li><strong>PipeDream (1F1B):</strong> This approach uses the “One-Forward-One-Backward” schedule. Once the pipeline warms up, workers alternate between processing a forward pass (storing a new microbatch of activations) and a backward pass (releasing an old microbatch of activations). The simulation highlights how this keeps memory usage stable and capped by the pipeline depth rather than the minibatch size per weight update. <ul> <li><em>Reference:</em> <a href="https://people.eecs.berkeley.edu/~matei/papers/2019/sosp_pipedream.pdf#page=4.10">SOSP ‘19: PipeDream: Generalized Pipeline Parallelism for DNN Training</a> (See their Figure 3 and Figure 4)</li> </ul> </li> </ol> <p>This tool is designed to be an interactive reference to better understand the scheduling concepts and memory implications discussed in the PipeDream paper.</p>]]></content><author><name></name></author><category term="technical"/><category term="pipeline-parallelism"/><category term="mlsys"/><category term="visual"/><summary type="html"><![CDATA[How 1F1B schedule in PipeDream reduces activation memory hold on GPU compared to naive PP schedule like GPipe.]]></summary></entry><entry><title type="html">How Thread Block Swizzling boosts L2 Cache Hit Rate in Matrix Multiplication</title><link href="https://weilixu.com/blog/2026/thread-block-swizzling/" rel="alternate" type="text/html" title="How Thread Block Swizzling boosts L2 Cache Hit Rate in Matrix Multiplication"/><published>2026-02-07T00:00:00+00:00</published><updated>2026-02-07T00:00:00+00:00</updated><id>https://weilixu.com/blog/2026/thread-block-swizzling</id><content type="html" xml:base="https://weilixu.com/blog/2026/thread-block-swizzling/"><![CDATA[<p>Understanding how data access patterns affect GPU cache performance is critical for writing efficient kernels. This interactive simulation demonstrates the impact of <strong>Thread Block Swizzling</strong> on L2 Cache hit rates in Matrix Multiplication.</p> <p><a href="https://how-thread-block-swizzling-boosts-l2-cache-hit-ra-952909165134.us-west1.run.app/">Launch Interactive Simulation</a></p> <h3 id="the-problem-row-major-access-in-computing-c--a-b">The Problem: Row-Major Access in Computing $C = A B$</h3> <p>In a standard <strong>Row-Major</strong> block execution order, the GPU processes the output matrix $C$ row-by-row. For each new row of tiles in $C$, the kernel potentially needs to reload the entire $B$ matrix. If matrix $B$ is larger than the L2 cache, this leads to cache thrashing, where useful data is evicted before it can be reused by subsequent blocks.</p> <h3 id="the-solution-swizzling-grouped-access-to-b">The Solution: Swizzling (Grouped Access to $B$)</h3> <p>Triton and other high-performance kernels allows <strong>Swizzling</strong> (or Grouped Access) to reorder the execution of thread blocks. By processing blocks in groups (e.g., essentially iterating in a column-major order within a small distinct group of rows), multiple blocks can reuse the same tiles of Matrix $B$ while they are still resident in the L2 cache.</p> <p>This simulation allows you to:</p> <ul> <li>Toggle between <strong>Row-Major</strong> and <strong>Grouped</strong> access patterns.</li> <li>Adjust the <strong>Group Size</strong> and <strong>L2 Cache Size</strong> to see the direct impact on hit rates.</li> <li>Watch the simulation step-by-step to see exactly when cache hits and DRAM loads occur.</li> </ul> <p>This serves as a companion to <a href="https://triton-lang.org/main/getting-started/tutorials/03-matrix-multiplication.html">Triton’s Official Matrix Multiplication Tutorial</a>, helping to visualize the <code class="language-plaintext highlighter-rouge">GROUP_SIZE_M</code> parameter implementation.</p>]]></content><author><name></name></author><category term="technical"/><category term="io-aware"/><category term="gpu"/><category term="triton"/><category term="mlsys"/><category term="visual"/><category term="kernel"/><summary type="html"><![CDATA[A visual aid to Triton's official matmul tutorial, explaining how thread block level swizzling increases L2 cache hit rate.]]></summary></entry><entry><title type="html">Implementing Flash Attention: Backward Pass in Triton</title><link href="https://weilixu.com/blog/2026/fa-bwd-triton/" rel="alternate" type="text/html" title="Implementing Flash Attention: Backward Pass in Triton"/><published>2026-01-30T12:00:00+00:00</published><updated>2026-01-30T12:00:00+00:00</updated><id>https://weilixu.com/blog/2026/fa-bwd-triton</id><content type="html" xml:base="https://weilixu.com/blog/2026/fa-bwd-triton/"><![CDATA[ <p>So you’ve seen the forward pass in FlashAttention, and you know how to compute the attention output without ever materializing the big $N \times N$ score matrix using tiling and online softmax.</p> <p>But in deep learning, computing the output is only half the battle. To train the model, we need gradients. We need the <strong>backward pass</strong>.</p> <p>Implementing a backward pass for FlashAttention is notoriously more difficult than the forward pass. Why? Because we didn’t save the massive attention score matrix. Instead, we <strong>recompute</strong> attention scores on the fly using the saved LogSumExp (<code class="language-plaintext highlighter-rouge">lse</code>) from the forward pass.</p> <p>In this walkthrough, we will dissect the backward pass in FLA’s excellent <a href="https://github.com/fla-org/flash-linear-attention/blob/f0de028bf3a4a6bb7c39f9c9585c4f03a7b72641/fla/ops/attn/parallel.py">implementation</a> in 3 phases.</p> <h2 id="the-data-layout">The Data Layout</h2> <p>Before we jump into the code, let’s explicitly define what we are working with. The kernel is designed to handle Grouped Query Attention (GQA), which made the shapes slightly more complicated.</p> <p>We operate on the following tensors:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">q</code> (Queries):</strong> Shape <code class="language-plaintext highlighter-rouge">[B, T, HQ, K]</code>. <ul> <li><code class="language-plaintext highlighter-rouge">B</code>: Batch size.</li> <li><code class="language-plaintext highlighter-rouge">T</code>: Sequence length.</li> <li><code class="language-plaintext highlighter-rouge">HQ</code>: Number of Query Heads.</li> <li><code class="language-plaintext highlighter-rouge">K</code>: Head dimension for keys/queries.</li> </ul> </li> <li><strong><code class="language-plaintext highlighter-rouge">k</code> (Keys):</strong> Shape <code class="language-plaintext highlighter-rouge">[B, T, H, K]</code>. <ul> <li><code class="language-plaintext highlighter-rouge">H</code>: Number of Key/Value Heads. Note that <code class="language-plaintext highlighter-rouge">HQ</code> must be a multiple of <code class="language-plaintext highlighter-rouge">H</code>.</li> </ul> </li> <li><strong><code class="language-plaintext highlighter-rouge">v</code> (Values):</strong> Shape <code class="language-plaintext highlighter-rouge">[B, T, H, V]</code>. <ul> <li><code class="language-plaintext highlighter-rouge">V</code>: Head dimension for values.</li> </ul> </li> <li><strong><code class="language-plaintext highlighter-rouge">do</code> (Output’s gradient):</strong> Shape <code class="language-plaintext highlighter-rouge">[B, T, HQ, V]</code>. <ul> <li>This is the incoming gradient from the next layer, with the same shape as the forward output.</li> </ul> </li> </ul> <p>Our goal is to produce <code class="language-plaintext highlighter-rouge">dq</code>, <code class="language-plaintext highlighter-rouge">dk</code>, and <code class="language-plaintext highlighter-rouge">dv</code>, matching the shapes of <code class="language-plaintext highlighter-rouge">q</code>, <code class="language-plaintext highlighter-rouge">k</code>, and <code class="language-plaintext highlighter-rouge">v</code> respectively.</p> <h2 id="the-big-picture-autograd">The Big Picture: Autograd</h2> <p>Inside the <code class="language-plaintext highlighter-rouge">ParallelAttentionFunction</code>, the <code class="language-plaintext highlighter-rouge">backward</code> method is automatically called by PyTorch when we call <code class="language-plaintext highlighter-rouge">optimizer.step()</code>.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@torch.compile</span>
<span class="k">class</span> <span class="nc">ParallelAttentionFunction</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="n">autograd</span><span class="p">.</span><span class="n">Function</span><span class="p">):</span>

    <span class="nd">@staticmethod</span>
    <span class="nd">@contiguous</span>
    <span class="nd">@autocast_custom_fwd</span>
    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">q</span><span class="p">,</span> <span class="n">k</span><span class="p">,</span> <span class="n">v</span><span class="p">,</span> <span class="n">g</span><span class="p">,</span> <span class="n">scale</span><span class="p">,</span> <span class="n">cu_seqlens</span><span class="p">):</span>
        <span class="n">ctx</span><span class="p">.</span><span class="n">dtype</span> <span class="o">=</span> <span class="n">q</span><span class="p">.</span><span class="n">dtype</span>
        <span class="n">RCP_LN2</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">1.4426950216</span>
        <span class="n">g_cumsum</span> <span class="o">=</span> <span class="nf">chunk_global_cumsum</span><span class="p">(</span><span class="n">g</span><span class="p">,</span> <span class="n">cu_seqlens</span><span class="o">=</span><span class="n">cu_seqlens</span><span class="p">,</span> <span class="n">scale</span><span class="o">=</span><span class="n">RCP_LN2</span><span class="p">)</span> <span class="k">if</span> <span class="n">g</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span> <span class="k">else</span> <span class="bp">None</span>
        <span class="n">o</span><span class="p">,</span> <span class="n">lse</span> <span class="o">=</span> <span class="nf">parallel_attn_fwd</span><span class="p">(</span>
            <span class="n">q</span><span class="o">=</span><span class="n">q</span><span class="p">,</span>
            <span class="n">k</span><span class="o">=</span><span class="n">k</span><span class="p">,</span>
            <span class="n">v</span><span class="o">=</span><span class="n">v</span><span class="p">,</span>
            <span class="n">g_cumsum</span><span class="o">=</span><span class="n">g_cumsum</span><span class="p">,</span>
            <span class="n">scale</span><span class="o">=</span><span class="n">scale</span><span class="p">,</span>
            <span class="n">cu_seqlens</span><span class="o">=</span><span class="n">cu_seqlens</span><span class="p">,</span>
        <span class="p">)</span>
        <span class="n">ctx</span><span class="p">.</span><span class="nf">save_for_backward</span><span class="p">(</span><span class="n">q</span><span class="p">,</span> <span class="n">k</span><span class="p">,</span> <span class="n">v</span><span class="p">,</span> <span class="n">o</span><span class="p">,</span> <span class="n">g_cumsum</span><span class="p">,</span> <span class="n">lse</span><span class="p">)</span>
        <span class="n">ctx</span><span class="p">.</span><span class="n">cu_seqlens</span> <span class="o">=</span> <span class="n">cu_seqlens</span>
        <span class="n">ctx</span><span class="p">.</span><span class="n">scale</span> <span class="o">=</span> <span class="n">scale</span>
        <span class="k">return</span> <span class="n">o</span><span class="p">.</span><span class="nf">to</span><span class="p">(</span><span class="n">q</span><span class="p">.</span><span class="n">dtype</span><span class="p">)</span>
</code></pre></div></div> <p>The context object <code class="language-plaintext highlighter-rouge">ctx</code> gives us back the tensors we saved during the forward pass. In particular, we have <code class="language-plaintext highlighter-rouge">o</code> (the forward output) and <code class="language-plaintext highlighter-rouge">lse</code> (the Log-Sum-Exp statistics). We pass these, along with the incoming gradient <code class="language-plaintext highlighter-rouge">do</code>, into our launcher <code class="language-plaintext highlighter-rouge">parallel_attn_bwd</code>.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="nd">@staticmethod</span>
    <span class="nd">@contiguous</span>
    <span class="nd">@autocast_custom_bwd</span>
    <span class="k">def</span> <span class="nf">backward</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">do</span><span class="p">):</span>
        <span class="n">q</span><span class="p">,</span> <span class="n">k</span><span class="p">,</span> <span class="n">v</span><span class="p">,</span> <span class="n">o</span><span class="p">,</span> <span class="n">g_cumsum</span><span class="p">,</span> <span class="n">lse</span> <span class="o">=</span> <span class="n">ctx</span><span class="p">.</span><span class="n">saved_tensors</span>
        <span class="n">dq</span><span class="p">,</span> <span class="n">dk</span><span class="p">,</span> <span class="n">dv</span><span class="p">,</span> <span class="n">dg</span> <span class="o">=</span> <span class="nf">parallel_attn_bwd</span><span class="p">(</span>
            <span class="n">q</span><span class="o">=</span><span class="n">q</span><span class="p">,</span>
            <span class="n">k</span><span class="o">=</span><span class="n">k</span><span class="p">,</span>
            <span class="n">v</span><span class="o">=</span><span class="n">v</span><span class="p">,</span>
            <span class="n">o</span><span class="o">=</span><span class="n">o</span><span class="p">,</span>
            <span class="n">g_cumsum</span><span class="o">=</span><span class="n">g_cumsum</span><span class="p">,</span>
            <span class="n">lse</span><span class="o">=</span><span class="n">lse</span><span class="p">,</span>
            <span class="n">do</span><span class="o">=</span><span class="n">do</span><span class="p">,</span>
            <span class="n">scale</span><span class="o">=</span><span class="n">ctx</span><span class="p">.</span><span class="n">scale</span><span class="p">,</span>
            <span class="n">cu_seqlens</span><span class="o">=</span><span class="n">ctx</span><span class="p">.</span><span class="n">cu_seqlens</span><span class="p">,</span>
        <span class="p">)</span>
        <span class="k">if</span> <span class="n">dg</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
            <span class="n">dg</span> <span class="o">=</span> <span class="nf">chunk_global_cumsum</span><span class="p">(</span><span class="n">dg</span><span class="p">,</span> <span class="n">cu_seqlens</span><span class="o">=</span><span class="n">ctx</span><span class="p">.</span><span class="n">cu_seqlens</span><span class="p">,</span> <span class="n">reverse</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>

        <span class="k">return</span> <span class="n">dq</span><span class="p">.</span><span class="nf">to</span><span class="p">(</span><span class="n">q</span><span class="p">),</span> <span class="n">dk</span><span class="p">.</span><span class="nf">to</span><span class="p">(</span><span class="n">k</span><span class="p">),</span> <span class="n">dv</span><span class="p">.</span><span class="nf">to</span><span class="p">(</span><span class="n">v</span><span class="p">),</span> <span class="n">dg</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span>
</code></pre></div></div> <h2 id="the-launcher">The Launcher</h2> <p>The launcher function <code class="language-plaintext highlighter-rouge">parallel_attn_bwd</code> handles the dispatch. Unlike the forward pass which is usually one big kernel, the backward pass here is split into <strong>three</strong> distinct operations.</p> <ol> <li><strong>Preprocessing:</strong> Calculate a helper term called <code class="language-plaintext highlighter-rouge">delta</code>.</li> <li><strong>dQ Kernel:</strong> Compute gradients for Queries (<code class="language-plaintext highlighter-rouge">dq</code>) and the log-decay <code class="language-plaintext highlighter-rouge">g</code> (<code class="language-plaintext highlighter-rouge">dg</code>).</li> <li><strong>dKV Kernel:</strong> Compute gradients for Keys (<code class="language-plaintext highlighter-rouge">dk</code>) and Values (<code class="language-plaintext highlighter-rouge">dv</code>).</li> </ol> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">parallel_attn_bwd</span><span class="p">(</span>
    <span class="c1"># arguments omitted here
</span><span class="p">):</span>
    <span class="c1"># extracting shapes
</span>    <span class="n">B</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="n">H</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="n">V</span> <span class="o">=</span> <span class="o">*</span><span class="n">k</span><span class="p">.</span><span class="n">shape</span><span class="p">,</span> <span class="n">v</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span>
    <span class="n">HQ</span> <span class="o">=</span> <span class="n">q</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span>
    <span class="n">G</span> <span class="o">=</span> <span class="n">HQ</span> <span class="o">//</span> <span class="n">H</span>  <span class="c1"># group size in GQA
</span>
    <span class="c1"># determine block sizes based on GPU architecture (Hopper vs Ampere)
</span>    <span class="k">if</span> <span class="nf">check_shared_mem</span><span class="p">(</span><span class="sh">'</span><span class="s">hopper</span><span class="sh">'</span><span class="p">):</span>
        <span class="n">BT</span> <span class="o">=</span> <span class="mi">128</span>
        <span class="n">BS</span> <span class="o">=</span> <span class="mi">64</span>
        <span class="c1"># ...
</span>    
    <span class="c1"># preprocess delta
</span>    <span class="n">delta</span> <span class="o">=</span> <span class="nf">parallel_attn_bwd_preprocess</span><span class="p">(</span><span class="n">o</span><span class="p">,</span> <span class="n">do</span><span class="p">)</span>

    <span class="c1"># prepare output tensors
</span>    <span class="c1"># dq: [B, T, HQ, K]
</span>    <span class="c1"># dk: [B, T, HQ, K]
</span>    <span class="c1"># dv: [B, T, HQ, V]
</span>    <span class="c1"># since we replicate G copies of keys/values in GQA
</span>    <span class="c1"># shape of dk and dv might not be the same as k and v, respectively
</span>    <span class="n">dq</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">empty</span><span class="p">(...,</span> <span class="n">device</span><span class="o">=</span><span class="n">q</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
    <span class="n">dk</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">empty</span><span class="p">(...,</span> <span class="n">device</span><span class="o">=</span><span class="n">q</span><span class="p">.</span><span class="n">device</span><span class="p">)</span> 
    <span class="n">dv</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">empty</span><span class="p">(...,</span> <span class="n">device</span><span class="o">=</span><span class="n">q</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
    
    <span class="c1"># launch separate kernels for dQ and dKV
</span>    <span class="n">grid</span> <span class="o">=</span> <span class="p">(</span><span class="n">NV</span><span class="p">,</span> <span class="n">NT</span><span class="p">,</span> <span class="n">B</span> <span class="o">*</span> <span class="n">HQ</span><span class="p">)</span>
    
    <span class="n">parallel_attn_bwd_kernel_dq</span><span class="p">[</span><span class="n">grid</span><span class="p">](</span>
        <span class="n">q</span><span class="o">=</span><span class="n">q</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="n">k</span><span class="p">,</span> <span class="n">v</span><span class="o">=</span><span class="n">v</span><span class="p">,</span> <span class="n">lse</span><span class="o">=</span><span class="n">lse</span><span class="p">,</span> <span class="n">delta</span><span class="o">=</span><span class="n">delta</span><span class="p">,</span> <span class="n">do</span><span class="o">=</span><span class="n">do</span><span class="p">,</span> <span class="n">dq</span><span class="o">=</span><span class="n">dq</span><span class="p">,</span> <span class="bp">...</span>
    <span class="p">)</span>
    
    <span class="n">parallel_attn_bwd_kernel_dkv</span><span class="p">[</span><span class="n">grid</span><span class="p">](</span>
        <span class="n">q</span><span class="o">=</span><span class="n">q</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="n">k</span><span class="p">,</span> <span class="n">v</span><span class="o">=</span><span class="n">v</span><span class="p">,</span> <span class="n">lse</span><span class="o">=</span><span class="n">lse</span><span class="p">,</span> <span class="n">delta</span><span class="o">=</span><span class="n">delta</span><span class="p">,</span> <span class="n">do</span><span class="o">=</span><span class="n">do</span><span class="p">,</span> <span class="n">dk</span><span class="o">=</span><span class="n">dk</span><span class="p">,</span> <span class="n">dv</span><span class="o">=</span><span class="n">dv</span><span class="p">,</span> <span class="bp">...</span>
    <span class="p">)</span>
    
    <span class="c1"># Handle GQA reduction
</span>    <span class="c1"># We computed gradients for replicated keys/values. Now we sum them up.
</span>    <span class="n">dk</span> <span class="o">=</span> <span class="nf">reduce</span><span class="p">(</span><span class="n">dk</span><span class="p">,</span> <span class="sh">'</span><span class="s">b t (h g) k -&gt; b t h k</span><span class="sh">'</span><span class="p">,</span> <span class="n">g</span><span class="o">=</span><span class="n">G</span><span class="p">,</span> <span class="n">reduction</span><span class="o">=</span><span class="sh">'</span><span class="s">sum</span><span class="sh">'</span><span class="p">)</span>
    <span class="n">dv</span> <span class="o">=</span> <span class="nf">reduce</span><span class="p">(</span><span class="n">dv</span><span class="p">,</span> <span class="sh">'</span><span class="s">b t (h g) v -&gt; b t h v</span><span class="sh">'</span><span class="p">,</span> <span class="n">g</span><span class="o">=</span><span class="n">G</span><span class="p">,</span> <span class="n">reduction</span><span class="o">=</span><span class="sh">'</span><span class="s">sum</span><span class="sh">'</span><span class="p">)</span>
    
    <span class="k">return</span> <span class="n">dq</span><span class="p">,</span> <span class="n">dk</span><span class="p">,</span> <span class="n">dv</span><span class="p">,</span> <span class="n">dg_cumsum</span>
</code></pre></div></div> <p>The design choice to split <code class="language-plaintext highlighter-rouge">dq</code> and <code class="language-plaintext highlighter-rouge">dkv</code> calculation into two kernels (unlike some implementations that fuse them) simplifies the logic regarding causal masking loops and reduces the amount of SRAM needed per thread block.</p> <h2 id="step-1-preprocessing-delta">Step 1: Preprocessing Delta</h2> <p>Before calculating gradients, we need a term <code class="language-plaintext highlighter-rouge">delta</code>. To derive the Softmax gradient, there is a row-wise summation term $\sum (O \cdot dO)$, which we pre-compute to avoid constant re-computation in the main loop below. (Note that recomputation of this term is more expensive than recomputing the attentions scores.)</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@triton.jit</span>
<span class="k">def</span> <span class="nf">parallel_attn_bwd_kernel_preprocess</span><span class="p">(</span>
    <span class="n">o</span><span class="p">,</span> <span class="n">do</span><span class="p">,</span> <span class="n">delta</span><span class="p">,</span>
    <span class="n">B</span><span class="p">:</span> <span class="n">tl</span><span class="p">.</span><span class="n">constexpr</span><span class="p">,</span> <span class="n">V</span><span class="p">:</span> <span class="n">tl</span><span class="p">.</span><span class="n">constexpr</span><span class="p">,</span>
<span class="p">):</span>
    <span class="n">i_n</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">program_id</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
    <span class="n">o_d</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">B</span><span class="p">)</span> <span class="c1"># B is block size for V dimension
</span>    <span class="n">m_d</span> <span class="o">=</span> <span class="n">o_d</span> <span class="o">&lt;</span> <span class="n">V</span>

    <span class="n">b_o</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">o</span> <span class="o">+</span> <span class="n">i_n</span> <span class="o">*</span> <span class="n">V</span> <span class="o">+</span> <span class="n">o_d</span><span class="p">,</span> <span class="n">mask</span><span class="o">=</span><span class="n">m_d</span><span class="p">,</span> <span class="n">other</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
    <span class="n">b_do</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">do</span> <span class="o">+</span> <span class="n">i_n</span> <span class="o">*</span> <span class="n">V</span> <span class="o">+</span> <span class="n">o_d</span><span class="p">,</span> <span class="n">mask</span><span class="o">=</span><span class="n">m_d</span><span class="p">,</span> <span class="n">other</span><span class="o">=</span><span class="mi">0</span><span class="p">).</span><span class="nf">to</span><span class="p">(</span><span class="n">tl</span><span class="p">.</span><span class="n">float32</span><span class="p">)</span>
    
    <span class="c1"># compute a tile of delta
</span>    <span class="c1"># delta = sum(output * grad_output)
</span>    <span class="n">b_delta</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">b_o</span> <span class="o">*</span> <span class="n">b_do</span><span class="p">)</span>

    <span class="n">tl</span><span class="p">.</span><span class="nf">store</span><span class="p">(</span><span class="n">delta</span> <span class="o">+</span> <span class="n">i_n</span><span class="p">,</span> <span class="n">b_delta</span><span class="p">.</span><span class="nf">to</span><span class="p">(</span><span class="n">delta</span><span class="p">.</span><span class="n">dtype</span><span class="p">.</span><span class="n">element_ty</span><span class="p">))</span>
</code></pre></div></div> <p>This kernel computes the dot product of <code class="language-plaintext highlighter-rouge">o</code> and <code class="language-plaintext highlighter-rouge">do</code> for every token and stores it in <code class="language-plaintext highlighter-rouge">delta</code>. This term is critical for correcting the gradient of the Softmax function.</p> <h2 id="step-2-computing-dq">Step 2: Computing dQ</h2> <p>Now comes the heavy lifting. <code class="language-plaintext highlighter-rouge">parallel_attn_bwd_kernel_dq</code> computes gradients with respect to <code class="language-plaintext highlighter-rouge">q</code>.</p> <h3 id="set-up">Set Up</h3> <p>The kernel loads a block of queries <code class="language-plaintext highlighter-rouge">q</code> (<code class="language-plaintext highlighter-rouge">BT x BK</code>), the incoming gradient <code class="language-plaintext highlighter-rouge">do</code> (<code class="language-plaintext highlighter-rouge">BT x BV</code>), the denominator <code class="language-plaintext highlighter-rouge">lse</code>, and our precomputed <code class="language-plaintext highlighter-rouge">delta</code> into SRAM. These stay in registers/SRAM for the duration of the inner loop because we are computing <code class="language-plaintext highlighter-rouge">dq</code> for this specific block of time <code class="language-plaintext highlighter-rouge">BT</code>.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="c1"># [BT, BK] - Query Block
</span>    <span class="n">b_q</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">p_q</span><span class="p">,</span> <span class="n">boundary_check</span><span class="o">=</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">))</span>
    <span class="c1"># [BT, BV] - Gradient of Output Block
</span>    <span class="n">b_do</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">p_do</span><span class="p">,</span> <span class="n">boundary_check</span><span class="o">=</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">))</span>
    <span class="c1"># [BT] - LogSumExp and Delta
</span>    <span class="n">b_lse</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">p_lse</span><span class="p">,</span> <span class="n">boundary_check</span><span class="o">=</span><span class="p">(</span><span class="mi">0</span><span class="p">,))</span>
    <span class="n">b_delta</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">p_delta</span><span class="p">,</span> <span class="n">boundary_check</span><span class="o">=</span><span class="p">(</span><span class="mi">0</span><span class="p">,))</span>

    <span class="c1"># Initialize accumulator for dq
</span>    <span class="n">b_dq</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">zeros</span><span class="p">([</span><span class="n">BT</span><span class="p">,</span> <span class="n">BK</span><span class="p">],</span> <span class="n">dtype</span><span class="o">=</span><span class="n">tl</span><span class="p">.</span><span class="n">float32</span><span class="p">)</span>
</code></pre></div></div> <h3 id="recomputation-along-the-main-loop">Recomputation along the main loop</h3> <p>We iterate over blocks of Keys (<code class="language-plaintext highlighter-rouge">k</code>) and Values (<code class="language-plaintext highlighter-rouge">v</code>). This looks very similar to the forward pass, but we are doing it to recover the attention probability matrix $P$.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="k">for</span> <span class="n">i_s</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">i_t</span> <span class="o">*</span> <span class="n">BT</span><span class="p">,</span> <span class="n">BS</span><span class="p">):</span>
        <span class="c1"># Ommitted: Load k [BK, BS] and v [BV, BS]
</span>        
        <span class="c1"># Recompute Attention Scores [BT, BS]
</span>        <span class="n">b_s</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">dot</span><span class="p">(</span><span class="n">b_q</span><span class="p">,</span> <span class="n">b_k</span><span class="p">)</span> <span class="o">*</span> <span class="n">scale</span> <span class="o">*</span> <span class="n">RCP_LN2</span>

        <span class="c1"># Recompute Probability P = exp(Score - LSE)
</span>        <span class="c1"># We mask out future tokens if causal
</span>        <span class="n">b_s</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">where</span><span class="p">((</span><span class="n">o_q</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">&gt;=</span> <span class="n">o_k</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="p">:])</span> <span class="o">&amp;</span> <span class="n">m_k</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="p">:],</span> <span class="n">b_s</span><span class="p">,</span> <span class="nf">float</span><span class="p">(</span><span class="sh">'</span><span class="s">-inf</span><span class="sh">'</span><span class="p">))</span>
        <span class="n">b_p</span> <span class="o">=</span> <span class="nf">exp2</span><span class="p">(</span><span class="n">b_s</span> <span class="o">-</span> <span class="n">b_lse</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">])</span>
        
        <span class="c1"># Compute Gradient of Attention Scores (dS)
</span>        <span class="c1"># [BT, BV] @ [BV, BS] -&gt; [BT, BS]
</span>        <span class="n">b_dp</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">dot</span><span class="p">(</span><span class="n">b_do</span><span class="p">,</span> <span class="n">b_v</span><span class="p">)</span>
        
        <span class="c1"># dS = P * (dP - delta)
</span>        <span class="n">b_ds</span> <span class="o">=</span> <span class="n">b_p</span> <span class="o">*</span> <span class="p">(</span><span class="n">b_dp</span><span class="p">.</span><span class="nf">to</span><span class="p">(</span><span class="n">tl</span><span class="p">.</span><span class="n">float32</span><span class="p">)</span> <span class="o">-</span> <span class="n">b_delta</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">])</span>
        
        <span class="c1"># Accumulate Gradient for Query (dQ)
</span>        <span class="c1"># [BT, BS] @ [BS, BK] -&gt; [BT, BK]
</span>        <span class="n">b_dq</span> <span class="o">+=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">dot</span><span class="p">(</span><span class="n">b_ds</span><span class="p">.</span><span class="nf">to</span><span class="p">(</span><span class="n">b_k</span><span class="p">.</span><span class="n">dtype</span><span class="p">),</span> <span class="n">tl</span><span class="p">.</span><span class="nf">trans</span><span class="p">(</span><span class="n">b_k</span><span class="p">))</span>
</code></pre></div></div> <h3 id="the-gradient-math">The Gradient Math</h3> <p>The code concisely implements the backward pass of Softmax-Attention.</p> <ol> <li><strong>Recompute <code class="language-plaintext highlighter-rouge">b_p</code></strong>: Using <code class="language-plaintext highlighter-rouge">q</code>, <code class="language-plaintext highlighter-rouge">k</code>, and <code class="language-plaintext highlighter-rouge">lse</code>, we recompute the attention probabilities on the fly.</li> <li><strong>Compute <code class="language-plaintext highlighter-rouge">b_ds</code></strong>: This represents a tile of $\frac{\partial L}{\partial S}$ (the gradient of the attention scores). The formula <code class="language-plaintext highlighter-rouge">P * (dot(do, v) - delta)</code> is the standard efficient softmax gradient implementation.<sup id="fnref:1"><a href="#fn:1" class="footnote" rel="footnote" role="doc-noteref">1</a></sup></li> <li><strong>Compute <code class="language-plaintext highlighter-rouge">b_dq</code></strong>: Since $S = QK^T$, the gradient flow tells us $dQ = dS \cdot K$. In Triton, this is <code class="language-plaintext highlighter-rouge">tl.dot(b_ds, tl.trans(b_k))</code>.</li> </ol> <h2 id="step-3-computing-dk-and-dv">Step 3: Computing dK and dV</h2> <p>The second kernel, <code class="language-plaintext highlighter-rouge">parallel_attn_bwd_kernel_dkv</code>, is the “transpose” of the first.</p> <p>In <code class="language-plaintext highlighter-rouge">dq</code>, we fixed a block of <strong>Queries</strong> (rows) and iterated over columns. Here, we fix a block of <strong>Keys/Values</strong> (columns) and iterate over the <strong>Queries</strong> (rows) that attended to them.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="c1"># Load the Key and Value blocks we want gradients for
</span>    <span class="c1"># [BT, BK]
</span>    <span class="n">b_k</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">p_k</span><span class="p">,</span> <span class="n">boundary_check</span><span class="o">=</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">))</span>
    <span class="c1"># [BT, BV]
</span>    <span class="n">b_v</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">p_v</span><span class="p">,</span> <span class="n">boundary_check</span><span class="o">=</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">))</span>
    
    <span class="c1"># Initialize accumulators
</span>    <span class="n">b_dk</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">zeros</span><span class="p">([</span><span class="n">BT</span><span class="p">,</span> <span class="n">BK</span><span class="p">],</span> <span class="n">dtype</span><span class="o">=</span><span class="n">tl</span><span class="p">.</span><span class="n">float32</span><span class="p">)</span>
    <span class="n">b_dv</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">zeros</span><span class="p">([</span><span class="n">BT</span><span class="p">,</span> <span class="n">BV</span><span class="p">],</span> <span class="n">dtype</span><span class="o">=</span><span class="n">tl</span><span class="p">.</span><span class="n">float32</span><span class="p">)</span>
    
    <span class="c1"># Iterate over Query blocks (i_s loop)
</span>    <span class="c1"># Note: We only loop over queries that come AFTER the current key (causal masking)
</span>    <span class="c1"># If I am a Key at time 10, only Queries at time &gt;= 10 cared about me.
</span>    <span class="k">for</span> <span class="n">i_s</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">i_t</span> <span class="o">*</span> <span class="n">BT</span><span class="p">,</span> <span class="nf">min</span><span class="p">((</span><span class="n">i_t</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span> <span class="o">*</span> <span class="n">BT</span><span class="p">,</span> <span class="n">T</span><span class="p">),</span> <span class="n">BS</span><span class="p">):</span>
        <span class="c1"># Ommitted: Load q, do, lse, delta ...
</span>
        <span class="c1"># Recompute Score (Transposed)
</span>        <span class="c1"># Note: We compute dot(K, Q^T) effectively
</span>        <span class="n">b_s</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">dot</span><span class="p">(</span><span class="n">b_k</span><span class="p">,</span> <span class="n">tl</span><span class="p">.</span><span class="nf">trans</span><span class="p">(</span><span class="n">b_q</span><span class="p">))</span> <span class="o">*</span> <span class="n">scale</span> <span class="o">*</span> <span class="n">RCP_LN2</span>
        
        <span class="c1"># Recompute P 
</span>        <span class="n">b_p</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">where</span><span class="p">((</span><span class="n">o_k</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">&lt;=</span> <span class="n">o_q</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="p">:])</span> <span class="o">&amp;</span> <span class="n">m_q</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="p">:],</span> <span class="nf">exp2</span><span class="p">(</span><span class="n">b_s</span> <span class="o">-</span> <span class="n">b_lse</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="p">:]),</span> <span class="mi">0</span><span class="p">)</span>
        
        <span class="c1"># Compute dV
</span>        <span class="c1"># dV += P^T * dO
</span>        <span class="c1"># [BT, BS] @ [BS, BV] -&gt; [BT, BV]
</span>        <span class="n">b_dv</span> <span class="o">+=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">dot</span><span class="p">(</span><span class="n">b_p</span><span class="p">.</span><span class="nf">to</span><span class="p">(</span><span class="n">b_do</span><span class="p">.</span><span class="n">dtype</span><span class="p">),</span> <span class="n">b_do</span><span class="p">)</span>
        
        <span class="c1"># Compute dS
</span>        <span class="c1"># We need dP = dO * V^T. 
</span>        <span class="c1"># In code: b_dp = dot(b_v, trans(b_do))
</span>        <span class="n">b_dp</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">dot</span><span class="p">(</span><span class="n">b_v</span><span class="p">,</span> <span class="n">tl</span><span class="p">.</span><span class="nf">trans</span><span class="p">(</span><span class="n">b_do</span><span class="p">))</span>
        
        <span class="n">b_ds</span> <span class="o">=</span> <span class="n">b_p</span> <span class="o">*</span> <span class="p">(</span><span class="n">b_dp</span> <span class="o">-</span> <span class="n">b_delta</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="p">:])</span>
        
        <span class="c1"># Compute dK
</span>        <span class="c1"># dK += dS^T * Q
</span>        <span class="c1"># [BT, BS] @ [BS, BK] -&gt; [BT, BK]
</span>        <span class="n">b_dk</span> <span class="o">+=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">dot</span><span class="p">(</span><span class="n">b_ds</span><span class="p">.</span><span class="nf">to</span><span class="p">(</span><span class="n">b_q</span><span class="p">.</span><span class="n">dtype</span><span class="p">),</span> <span class="n">b_q</span><span class="p">)</span>
</code></pre></div></div> <p>The logic mirrors the <code class="language-plaintext highlighter-rouge">dq</code> kernel, but the matrices are transposed. For example, to get <code class="language-plaintext highlighter-rouge">dv</code>, we are essentially performing $P^T \cdot dO$.</p> <p>By keeping <code class="language-plaintext highlighter-rouge">k</code> and <code class="language-plaintext highlighter-rouge">v</code> in SRAM and streaming <code class="language-plaintext highlighter-rouge">q</code>, <code class="language-plaintext highlighter-rouge">do</code>, and <code class="language-plaintext highlighter-rouge">delta</code> from HBM, we efficiently compute the gradients for the Key and Value matrices. Finally, the launcher script will take these results and <code class="language-plaintext highlighter-rouge">reduce</code> them if we are using GQA, summing the gradients across the replicated heads.</p> <h2 id="acknowledgments">Acknowledgments</h2> <p>This code walkthrough is inspired by our very amazing <a href="https://nathanchen.me/">Nathan Chen</a>’s <a href="https://nathanchen.me/public/Triton-Flash-Attention-Kernel-Walkthrough.html">Triton Flash Attention Kernel Walkthrough: The Forward Pass</a>. Big shoutout to Nathan!</p> <hr/> <h3 id="footnote">Footnote</h3> <div class="footnotes" role="doc-endnotes"> <ol> <li id="fn:1"> <p><strong>The Delta Term derivation:</strong> If we define attention output $O_i = \sum_j P_{ij} V_j$, the gradient of the loss function $L$ with respect to the pre-softmax scores $S_{ij}$ involves the Jacobian of the Softmax. Specifically, $\frac{\partial L}{\partial S_{ij}} = P_{ij} (\frac{\partial L}{\partial P_{ij}} - \sum_k P_{ik} \frac{\partial L}{\partial P_{ik}})$.</p> <p>Since $\frac{\partial L}{\partial P_{ij}} = dO_i \cdot V_j$, we can substitute: $\frac{\partial L}{\partial S_{ij}} = P_{ij} ( (dO_i \cdot V_j) - \sum_k P_{ik} (dO_i \cdot V_k) )$.</p> <p>The term $\sum_k P_{ik} (dO_i \cdot V_k)$ can be rewritten by factoring out $dO_i$: $dO_i \cdot (\sum_k P_{ik} V_k) = dO_i \cdot O_i$.</p> <p>This term $dO_i \cdot O_i$ is exactly what we calculate in the <code class="language-plaintext highlighter-rouge">preprocess</code> kernel and store as <code class="language-plaintext highlighter-rouge">delta</code>. So, $dS_{ij} = P_{ij} ( (dO_i \cdot V_j) - \text{delta}_i )$. This matches the code: <code class="language-plaintext highlighter-rouge">b_ds = b_p * (b_dp - b_delta)</code>. <a href="#fnref:1" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> </ol> </div>]]></content><author><name></name></author><category term="technical"/><category term="attention"/><category term="gpu"/><category term="io-aware"/><category term="kernel"/><category term="mlsys"/><category term="triton"/><summary type="html"><![CDATA[In this follow-up post to Nathan Chen's Triton Flash Attention Kernel Walkthrough: The Forward Pass, we dive into gradient computation for queries, keys, and values in Flash Attention's backward pass.]]></summary></entry><entry><title type="html">View Transformer Layers from Online Optimization Perspective</title><link href="https://weilixu.com/blog/2025/view-transformer-layers/" rel="alternate" type="text/html" title="View Transformer Layers from Online Optimization Perspective"/><published>2025-07-17T12:00:00+00:00</published><updated>2025-07-17T12:00:00+00:00</updated><id>https://weilixu.com/blog/2025/view-transformer-layers</id><content type="html" xml:base="https://weilixu.com/blog/2025/view-transformer-layers/"><![CDATA[<p><a href="https://wenhaochai.com/assets/file/blog/mesa.pdf">Read the full PDF</a></p> <h3 id="the-fast-weight-perspective">The Fast Weight Perspective</h3> <p>The core idea is to view the <strong>hidden state</strong> of linear attention models not just as a memory buffer, but as a set of <strong>Fast Weights</strong> that are dynamically updated during the forward pass. This contrasts with the “Slow Weights” (the model parameters) learned during training.</p> <p>From this <strong>Online Optimization Perspective</strong>, the state update rule in architectures like <strong>Linear Attention</strong>, <strong>DeltaNet</strong>, and <strong>TTT (Test-Time Training)</strong> can be interpreted as a step of gradient descent on a specific objective function. For example, standard linear attention effectively minimizes the alignment error between the projected query and the target value stored in the state.</p> <h3 id="unifying-architectures">Unifying Architectures</h3> <p>We provide a framework that connects several recently emerged architectures:</p> <ul> <li><strong>Linear Attention Transformers</strong> (e.g. GLA, DeltaNet)</li> <li><strong>State-Space Models</strong> (e.g. Mamba)</li> <li><strong>RNN variants</strong> (e.g. RWKV)</li> </ul> <p>By understanding these models as a family of parametric systems that update their internal representation based on the input stream, we can better understand their capabilities and how this mechanism is related to their continual learning capabilities.</p>]]></content><author><name></name></author><category term="technical"/><category term="attention"/><category term="transformer"/><category term="sequence-modeling"/><category term="llm-architecture"/><category term="optimization"/><summary type="html"><![CDATA[In this blog post co-authored with Wenhao Chai, we revisit the landscape of efficient Transformer variants from a unified view of fast weight programming.]]></summary></entry></feed>